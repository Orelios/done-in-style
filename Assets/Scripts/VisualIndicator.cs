using UnityEngine;

public class VisualIndicator : MonoBehaviour
{
    /*private PlayerGearSwapper _gearSwapper;

    private void Awake()
    {
        _gearSwapper = GetComponentInParent<PlayerGearSwapper>();
    }

    private void Update()
    {
        SwitchSprites(); 
    }
    public void SwitchSprites()
    {

        if (_gearSwapper.CurrentGearEquipped.DaredevilGearType == EDaredevilGearType.Skateboard)
        { gameObject.GetComponent<SpriteRenderer>().color = Color.blue; }
        else if (_gearSwapper.CurrentGearEquipped.DaredevilGearType == EDaredevilGearType.RollerBlades)
        { gameObject.GetComponent<SpriteRenderer>().color = Color.red; ; }
        else if (_gearSwapper.CurrentGearEquipped.DaredevilGearType == EDaredevilGearType.PogoStick)
        { gameObject.GetComponent<SpriteRenderer>().color = Color.green; }
    }*/
}
